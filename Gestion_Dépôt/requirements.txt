
# Variables d'environnement (.env)
python-dotenv==1.2.2

# Chiffrement des IDs dans les URLs
hashids==1.3.1

# API Mobile
djangorestframework==3.17.1
djangorestframework-simplejwt==5.5.0
django-cors-headers==4.7.0

# Export PDF (fiche mouvement + listes)
xhtml2pdf

# Cachet virtuel + protection des PDF envoyés aux marketeurs
pypdf
cryptography

# Export PDF fidèle au navigateur (bordereaux de mouvement) — après installation,
# exécuter aussi : python -m playwright install chromium
playwright
